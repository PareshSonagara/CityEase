# ── Dependencies ─────────────────────────
node_modules/
client/node_modules/
server/node_modules/

# ── Build Output ──────────────────────────
client/dist/
client/.vite/

# ── SQLite Database (runtime-generated) ───
server/data/cityease.db
server/data/cityease.db-shm
server/data/cityease.db-wal

# ── Environment Variables ─────────────────
.env
.env.local
.env.production
server/.env

# ── Logs ──────────────────────────────────
logs/
*.log
npm-debug.log*

# ── OS & Editor Junk ──────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/

# ── Package Locks (keep one, ignore dupes) ─
# Keeping root & client locks; excluding server's
server/package-lock.json
